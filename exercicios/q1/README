1)	O diretório q1 possui código que permite calcular o valor de Pi por um método conhecido como Monte Carlo. Neste código são lançados diversos números aleatório que conformem caem dentro ou fora de um círculo permitem um cálculo aproximado do valor de Pi. O código já possui algumas partes vetorizadas, contudo o processo de elevar ao quadrado os valores do vetor e depois soma-los não foi implementado. Ajuste o programa usando instruções intrínsecas de compilação para processadores que suportam AVX2 ou superior. 
